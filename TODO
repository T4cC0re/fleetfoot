every vlan is to be a bridge. Interfaces can be attached to a bridge.
requires network + openssh and python on arch linux host to bootstrap
golang daemon instead of ansible
vpnc
ipoib
full IPv6 support (also 4to6)
add avahi reflector + config
upnp igd support
save current systemd overrides for ppp, avahi and other stuff to ansible
